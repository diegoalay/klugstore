import { defineBoot } from '#q-app/wrappers'

// Font Awesome Pro - todas las familias (solid, regular, light, thin, brands, duotone)
import '@fortawesome/fontawesome-pro/css/all.min.css'

export default defineBoot(() => {
  // Font Awesome Pro se carga automáticamente via el CSS import
})
